# vpython
A thread- and process-safe `python` wrapper/launcher script that exposes a `python`-compatible CLI, sets up/activates a virtual environment, and delegates execution to a target Python interpreter
